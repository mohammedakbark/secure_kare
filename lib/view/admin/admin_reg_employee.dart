import 'package:flutter/material.dart';

class AdminRegEmployee extends StatelessWidget {
  const AdminRegEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Reg"),
      ],
    );
  }
}
